# Next Steps

Generate QrCode to be scan on the computer == Done
let the computer join the room

make the first page of the phone to scan QrCode as a client == Done

Move the Spreadsheet to its own route == Done

Make the computer receive the isbn and call the corresponding function

update state with the corresponding room check if the format received from the qrcode matches the format generated "regex? .startsWith?"
connect to the room

==============
Finish the accession number inputting == Done

=============

## Client Side

Add an abort controller for timed input
make it offline accessible, make it a progressive web app
add background worker for syncing database to online database
add background service worker for sync fixing the resultant spreadsheet
Add book view page with lazy loading and infinite scrolling
fix saving both small and large image links
fix database supporting both

## Server Side

use socket for communication
add user entry for database
fix the resultant paddle api code
sync the spreadsheet with the database
and auto update the view
auto backup the database every day to google drive (api??)
generate random image for profile pic

## Ai Application

run ml inference locally
